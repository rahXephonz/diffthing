---
name: explore-codebase
description: Understand repository architecture with graph-first navigation
---

# Explore codebase

1. Read `AGENTS.md` and `docs/ARCHITECTURE.md`.
2. Request minimal architecture context from repository graph.
3. Identify relevant communities, entry points, and execution flows.
4. Trace targeted imports and call relationships.
5. Use `rg` and focused file reads to verify graph results.

Return concise component map, key invariants, and relevant file paths.
